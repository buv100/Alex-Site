# מסמך אפיון מלא — אלכס נכסים

| שדה | ערך |
|-----|-----|
| גרסה | 1.0 |
| תאריך | 2026-07-31 |
| סטטוס | מאושר כבסיס לפיתוח (תוכן עסקי/תמונות יגיעו בהמשך) |
| מוצר | אתר אינטרנט WEB (אפליקציה — שלב נפרד) |
| מותג | אלכס נכסים |
| בעל העסק | אלכס (מתווך נדל״ן אישי, ירושלים) |
| מפתח / אפוטרופוס טכני | (מנהל הפרויקט הטכני) |

**מסמכים קשורים:** [PROJECT_RULES.md](./PROJECT_RULES.md) · [DATA_CHECKLIST.md](./DATA_CHECKLIST.md)

**הצהרת אחריות:** סעיפי נגישות ופרטיות במסמך זה מתארים דרישות יישום טכניות על בסיס דין ישראלי ידוע (ת״י 5568 / תקנות נגישות לשירות; חוק הגנת הפרטיות ותיקון 13). זה **אינו ייעוץ משפטי**. לפני עלייה עם לידים אמיתיים מומלץ אישור עו״ד לטיוטות משפטיות.

---

## 1. מטרת המערכת ומדדי הצלחה

### 1.1 חזון
אתר פשוט ונקי למותג האישי **אלכס נכסים**: הצגת דירות מגורים בירושלים (מכירה ושכירות), יצירת קשר נוחה עם אלכס, ופניות נוחות לאלכס.

### 1.2 מטרות עסקיות (לפי עדיפות)
1. **נוחות לרשת הקיימת** של אלכס — במקום שיחות אינסופיות ושליחת תמונות/פרטים ידנית.
2. **נוחות לבעל העסק** — פרסום נכסים ומעקב פניות בתהליך פנימי (לא חלק מהאתר הציבורי).
3. **לידים** — פנייה לנכס ספציפי, פנייה כללית, ולידים ממוכרים שרוצים לפרסם נכס.
4. **חשיפה לקהל חדש** — משנית בשלב זה; לא דוחפת SEO אגרסיבי או פרסום ממומן.

### 1.3 מדדי הצלחה (KPI)
| KPI | הגדרה |
|-----|--------|
| לידים חדשים | מספר פניות טופס / שבוע |
| המרות WhatsApp | לחיצות על כפתור WhatsApp מנכס |
| זמן פרסום נכס | מאלכס מתחיל העלאה עד `published` (יעד: נוח במובייל, דקות בודדות) |
| שימוש במועדפים | משתמשים רשומים ששומרים נכסים |
| שביעות רצון תפעולית | אלכס מצליח לנהל לבד מהטלפון בלי עזרה יומיומית |

### 1.4 מה האתר **אינו**
- לא מתחרה ליד2 / מדלן (אין חיפוש טקסט חופשי מאגר ענק).
- לא פלטפורמת תיווך מרובת מתווכים.
- לא אפליקציה native בשלב זה.
- לא CMS לתוכן שיווקי דינמי (טקסטים סטטיים בקוד).

---

## 2. בעלי עניין ותפקידים

| תפקיד | מי | הרשאות |
|--------|-----|--------|
| בעל העסק | אלכס | מספק ומאשר תוכן נכסים לפרסום (תהליך פנימי שאינו מתועד כאן) |
| מפתח | הבן / אפוטרופוס טכני | קוד, דיפלוי, שינוי טקסטים סטטיים, סיסמאות תשתית |
| מבקר | גולש לא מחובר | צפייה בנכסים ציבוריים, סינון, WhatsApp, טופס, הרשמה |
| משתמש רשום | לקוח / מכר של אלכס | כמו מבקר + מועדפים |
| מוכר פוטנציאלי | מי שרוצה לפרסם נכס | טופס "יש לי נכס" (אין הרשאת פרסום עצמית) |

**כלל ברזל:** רק אלכס מפרסם נכסים. אף משתמש ציבורי לא יכול להעלות או לערוך נכס.

---

## 3. תרחישי משתמש (User Stories + Flows)

### 3.1 מבקר — גלישה ופנייה לנכס
1. נכנס לדף הבית → רואה מותג, הסבר על אלכס, CTA לנכסים / יצירת קשר.
2. עובר לרשימת נכסים → מסנן לפי שכונה / מחיר / חדרים / סוג עסקה / תגיות.
3. נכנס לדף נכס → רואה תמונות ופרטים (בלי כתובת מדויקת).
4. בוחר:
   - **WhatsApp:** פותח צ׳אט עם הודעה מוכנה על הנכס.
   - **טופס:** שם + טלפון (+ הסכמת פרטיות) → ליד נשמר + התראה לאלכס.

### 3.2 מבקר — פנייה כללית
1. "צור קשר" → טופס שם + טלפון / או קישור WhatsApp כללי / `tel:`.
2. ליד מסוג `general` בלי `propertyId`.

### 3.3 מוכר פוטנציאלי
1. "יש לי נכס למכירה/השכרה" → טופס שם, טלפון, תיאור קצר, סוג עסקה רצוי.
2. ליד מסוג `seller` → אלכס חוזר ידנית.

### 3.4 משתמש רשום — מועדפים
1. הרשמה מהירה (פחות מחצי דקה) → חשבון.
2. שמירת נכס למועדפים / הסרה.
3. צפייה ברשימת מועדפים מהפרופיל.

### 3.5 אלכס — פרסום נכס מהטלפון
1. בעל העסק מוסיף נכס בתהליך פנימי.
2. "נכס חדש" → מילוי שדות ציבוריים + פנימיים → העלאת תמונות מהגלריה.
3. שמירה כ־`draft` → תצוגה מקדימה → `published`.
4. מאוחר יותר: סימון `sold` / `rented` → מעבר לארכיון "הנכסים שמכרנו".

### 3.6 אלכס — טיפול בליד
1. דשבורד / תיבת לידים → רואה ליד חדש (כולל קישור לנכס אם רלוונטי).
2. מעדכן סטטוס: `new` → `in_progress` → `closed`.
3. יוצר קשר מחוץ לאתר (שיחה / WhatsApp) — **ישירות מכפתורי טלפון / וואטסאפ**.

### 3.8 נוחות ללקוחות קיימים (יעד מוצר)
האתר נועד גם לרשת הקיימת של אלכס — לא רק לגולשים חדשים:

| יכולת | למי | סטטוס |
|--------|-----|--------|
| שליחת קישור נכס בוואטסאפ (טקסט מוכן) | אלכס → לקוח | v1 |
| מועדפים + הרשמה מהירה | לקוח חוזר | v1 |
| סינון שכונה/מחיר/חדרים בלי חיפוש חופשי | לקוח | v1 |
| ארכיון "הנכסים שמכרנו" (אמון) | לקוח | v1 |
| כפתור התקשר / וואטסאפ מכל נכס | לקוח | v1 |
| שיתוף קישור נכס ללקוח (העתקה / WA) | אלכס | v1 |
| שמירת העדפות סינון (local) | לקוח | Phase מאוחר |
| התראת וואטסאפ ללקוח על נכס חדש מתאים | אלכס שולח ידנית / בעתיד בוט מסייע | לא אוטומציה מלאה ב־v1 |
| בוט צ׳אט לשאלות על נכסים ועל אלכס | לקוח | ראה §3.9 — מתוכנן |

### 3.9 בוט צ׳אט (מתוכנן — לא v1 מיידי)
**מטרה:** מענה מיידי בעברית לשאלות נפוצות על נכסים פעילים ועל אלכס, ולהפחית שיחות חוזרות — בלי להחליף את אלכס בעסקאות.

**כללי ברזל:**
1. עונה רק על מידע **ציבורי** (אותם שדות כמו באתר). אסור לחשוף `owner*`, `exactAddress`, `internalNotes`, `minPriceNegotiable`.
2. על מחיר חסר → מפנה ל"צור קשר לבירור" / וואטסאפ לאלכס.
3. שאלות מחוץ להיקף / מו״מ → הסלמה ל־WhatsApp / טופס ליד.
4. עברית בלבד בצד הציבורי.
5. תקציב: **חינמי בלבד** אלא אם יאושר מפורשות שירות בתשלום.
6. פרטיות: לא לשמור שיחות עם PII בלי הסכמה; עדיף שיחה אפיזודית + לוג פנימי אגרגטיבי בלבד (אם בכלל).

**גישת מימוש מוצעת (שלבים):**
| שלב | תוכן | הערות |
|-----|------|--------|
| A | FAQ קבוע על אלכס + קישורי נכסים לפי מסננים פשוטים (שכונה / מכירה-שכירות) | בלי LLM, חינם |
| B | Retrieval על תיאורי נכסים מפורסמים + תשובות מובנות | עדיין ללא עלות API |
| C | LLM בשכבת free-tier (רק אם יאושר) מעל אותם מקורות | עם הגבלת קצב + disclaimer |

**מיקום UI:** כפתור צ׳אט צף באתר הציבורי (לא חוסם נגישות; ניתן לסגירה במקלדת).


---

## 4. מפת אתר (Information Architecture)

### 4.1 ציבורי (עברית, RTL)

| נתיב (לוגי) | תיאור |
|-------------|--------|
| `/` | דף בית — מותג, על אלכס, CTA |
| `/properties` | רשימת נכסים פעילים + מסננים |
| `/properties/[id]` | דף נכס בודד |
| `/archive` | ארכיון "הנכסים שמכרנו" (תצוגה ציבורית מצומצמת) |
| `/about` | על אלכס / למה לעבוד איתו (אפשר גם כסקשן בבית) |
| `/contact` | צור קשר |
| `/sell` | יש לי נכס לפרסום |
| `/auth/register` | הרשמה |
| `/auth/login` | התחברות משתמש |
| `/favorites` | מועדפים (דורש התחברות) |
| `/privacy` | מדיניות פרטיות |
| `/cookies` | מדיניות עוגיות (חיוניות; אין אנליטיקס ב־v1) |
| `/terms` | תנאי שימוש |
| `/accessibility` | הצהרת נגישות |
| `/disclaimer` | דיסקליימר מחירים/זמינות (או כחלק מתקנון) |


---

## 5. UX / UI — עקרונות עיצוב

### 5.1 כיוון ויזואלי
- יוקרתי, פשוט, ייחודי — **לא** מראה AI גנרי.
- צבעים כהים יחסית אך לא קיצוניים (רקע כהה-מתון, טקסט בהיר, אקסנט חם/זהוב או כחול-עמוק — יוגדר ב־CSS variables).
- טיפוגרפיה אקספרסיבית (לא Inter/Roboto/Arial כברירת מחדל).
- תמונות נכסים אמיתיות בלבד (אלכס מעלה).

### 5.2 דף הבית — viewport ראשון
חייב לכלול:
1. שם המותג **אלכס נכסים** כסיגנל hero.
2. הסבר קצר על אלכס ולמה כדאי לעבוד איתו.
3. קבוצת CTA (למשל: לצפייה בנכסים / צור קשר).
4. עוגן ויזואלי (תמונת אלכס / אווירת נדל״ן בירושלים) — full-bleed לפי כללי העיצוב של הפרויקט.

**לא** ב־viewport הראשון: סטטיסטיקות, רשימות אירועים, כרטיסיות מרובות, באנרים צפים.

### 5.3 Mobile-first
- כל המסכים הציבוריים מותאמים לטלפון קודם.

### 5.4 שיתוף
- כפתור שיתוף WhatsApp בכל דף נכס (לינק + טקסט קצר).

### 5.5 שפות
| אזור | שפה |
|------|-----|
| אתר ציבורי | עברית בלבד |
| כלי פנימי | לא מתועד בריפו הציבורי |
| אנגלית | אין באתר הציבורי |

---

## 6. מודל נתונים (Postgres / Neon)

### 6.1 `Property`

#### שדות ציבוריים (מוצגים באתר)
| שדה | סוג | חובה | הערות |
|-----|-----|------|--------|
| `title` | string | כן | כותרת |
| `description` | string | כן | טקסט פשוט + שורות חדשות |
| `dealType` | enum: `sale` \| `rent` | כן | מכירה / שכירות |
| `propertyType` | enum | כן | דירה, פנטהאוז, דופלקס, גן, סטודיו, אחר (דירות מגורים) |
| `status` | enum | כן | ראה סעיף 7 |
| `price` | number \| null | לא | אם null → הצג "צור קשר לבירור" |
| `currency` | string | כן | ברירת מחדל `ILS` |
| `rooms` | number | כן | |
| `sizeSqm` | number | מומלץ | שטח במ״ר |
| `floor` | number \| null | לא | |
| `totalFloors` | number \| null | לא | |
| `hasElevator` | boolean | לא | |
| `hasParking` | boolean | לא | |
| `hasBalcony` | boolean | לא | |
| `direction` | string \| null | לא | כיווני אוויר |
| `city` | string | כן | ברירת מחדל `ירושלים` |
| `neighborhood` | string | כן | שכונה |
| `street` | string \| null | לא | **שם רחוב בלבד** — בלי מספר בניין/דירה |
| `arnona` | number \| null | לא | ארנונה (₪) |
| `vaadBayit` | number \| null | לא | ועד בית (₪) |
| `areaPopulationNotes` | string \| null | לא | תיאור אוכלוסייה / אופי האזור |
| `isOpportunity` | boolean | כן | תג "בהזדמנות" |
| `isExclusive` | boolean | כן | תג "בלעדיות" |
| `images` | array of `{ url, publicId?, alt, order }` | כן לפרסום | לפחות תמונה אחת ב־`published` |
| `publishedAt` | date \| null | — | |
| `archivedAt` | date \| null | — | |
| `createdAt` / `updatedAt` | date | כן | |

#### שדות פנימיים (לעולם לא ב־API/HTML ציבורי)
| שדה | סוג | הערות |
|-----|-----|--------|
| `ownerName` | string \| null | בעל הנכס |
| `ownerPhone` | string \| null | |
| `ownerNotes` | string \| null | הערות פנימיות |
| `minPriceNegotiable` | number \| null | מחיר מינימום למו״מ |
| `internalNotes` | string \| null | הערות אלכס |
| `exactAddress` | string \| null | כתובת מלאה פנימית (אופציונלי) |
| `deletedAt` | date \| null | soft-delete |

**אין** מזהה נכס ציבורי נפרד (קוד נכס) — משתמשים ב־`id` (UUID) פנימי / slug אופציונלי בעתיד.

### 6.2 `User` (משתמש ציבורי)
| שדה | סוג | הערות |
|-----|-----|--------|
| `name` | string | |
| `phone` | string | ייחודי מומלץ |
| `email` | string \| null | אופציונלי |
| `passwordHash` | string | |
| `favorites` | string[] (UUID של נכסים) | או אוסף נפרד |
| `createdAt` | date | |
| `privacyConsentAt` | date | חותמת הסכמה |

הרשמה פשוטה: שם + טלפון + סיסמה קצרה (+ צ׳קבוקס פרטיות). אבטחה בסיסית מספיקה (אין מידע רגיש מעבר למועדפים ופרטי קשר).

### 6.3 `Lead`
| שדה | סוג | הערות |
|-----|-----|--------|
| `type` | enum: `property` \| `general` \| `seller` | |
| `name` | string | |
| `phone` | string | |
| `message` | string \| null | |
| `propertyId` | string (UUID) \| null | חובה ל־`property` |
| `propertyTitle` | string \| null | snapshot |
| `propertyUrl` | string \| null | snapshot |
| `status` | enum: `new` \| `in_progress` \| `closed` | |
| `source` | enum: `form` \| `whatsapp_click` (אופציונלי לסטטיסטיקה) | |
| `privacyConsentAt` | date | |
| `createdAt` / `updatedAt` | date | |

### 6.4 הגדרות מערכת
- הרשאות כתיבה מוגנות ב־env בשרת (לא בקומיט).
- אופציונלי: `AuditLog` (login, publish, delete) לשלב מאוחר.

### 6.5 סטטיסטיקות (מינימום)
שמירה מצטברת או חישוב מ־DB:
- מספר נכסים פעילים / בארכיון
- מספר לידים לפי סטטוס / לפי יום
- צפיות לדף נכס (אופציונלי ב־Phase 2 — מונה פשוט)

---

## 7. ביזנס לוגיק וכללים

### 7.1 מחזור חיים של נכס
```
draft → (preview) → published → sold | rented → archive
                              ↘ soft-delete (deletedAt)
```

| סטטוס | נראה בציבור | הערות |
|--------|-------------|--------|
| `draft` | לא | טיוטה; לא ציבורי |
| `published` | כן ברשימה הראשית | חייב ≥1 תמונה |
| `sold` / `rented` | בארכיון "הנכסים שמכרנו" | לא ברשימה הראשית |
| soft-deleted | לא | ניתן לשחזור פנימית |

### 7.2 כללי פרסום
1. רק אלכס יכול לפרסם / לשנות סטטוס ל־`published`.
2. בלי תמונה אחת לפחות → **אין** מעבר ל־`published`.
3. מחיר ריק/null → תצוגה: **"צור קשר לבירור"** (לא ₪0).
4. כתובת ציבורית: עיר + שכונה + (אופציונלי) שם רחוב — **בלי** מספר בניין/דירה.
5. מיקוח על מחיר — רק דרך אלכס (מחוץ לאתר / הודעה).
6. תגיות `isOpportunity` / `isExclusive` מוצגות כתגים ויזואליים.
7. מיון ברשימת נכסים פעילים: **אקראי בכל טעינת עמוד** (אין ברירת מחדל של חדש ביותר).
8. אין הגבלת מספר נכסים פעילים (צפי: 5–15).

### 7.3 סינון (בלי חיפוש טקסט)
מסננים נתמכים:
- סוג עסקה (`sale` / `rent`)
- שכונה בירושלים
- טווח מחיר
- מספר חדרים
- שטח (אופציונלי)
- תג הזדמנות / בלעדיות
- (אופציונלי) מעלית / חניה / מרפסת

### 7.4 פניות
| סוג | מקור | שמירה ב־DB | התראה לאלכס |
|-----|------|------------|-------------|
| נכס ספציפי | WhatsApp prefill | אופציונלי (לחיצה) | ההודעה מגיעה בוואטסאפ |
| נכס ספציפי | טופס | כן + `propertyId` | אימייל ו/או WhatsApp |
| כללי | טופס / WhatsApp | כן אם טופס | כנ״ל |
| מוכר | טופס `/sell` | כן `seller` | כנ״ל |

**אין** תשובה אוטומטית לפונה ב־MVP. **אין** CAPTCHA ב־MVP (אפשר rate-limit בסיסי בשרת).

### 7.5 תבנית הודעת WhatsApp לנכס
```
שלום אלכס, אני מעוניין/ת בנכס: {title}
שכונה: {neighborhood}
קישור: {url}
```

מספר היעד: מספר WhatsApp הפרטי של אלכס (יועבר ב־DATA_CHECKLIST) דרך `https://wa.me/{number}?text=...`.

### 7.6 מועדפים
- דורשים משתמש מחובר.
- שמירה ב־DB לפי `userId` + `propertyId`.
- נכס שנמחק/לא פעיל: מוצג כלא זמין או מוסר אוטומטית.

---

## 8. פרסום תוכן

פרסום ועדכון נכסים נעשים ע״י בעל העסק בתהליך שאינו מתועד בריפו הציבורי. טקסטים שיווקיים סטטיים באתר — בקוד, דרך המפתח.


## 9. טכנולוגיות ותשתית (תקציב 0 ₪)

### 9.1 Stack שננעל
| שכבה | בחירה | הערות |
|------|--------|--------|
| Frontend/Backend | Next.js (App Router) + TypeScript | |
| עיצוב | Tailwind CSS + CSS variables למותג | |
| DB | **Neon Postgres Free** (או Mongo מנוהל חינמי אם זמין) | `DATABASE_URL` — מומלץ Neon כי Atlas Free לא תמיד מופיע בממשק |
| Hosting | Vercel Free | `*.vercel.app` בהתחלה |
| תמונות | Cloudinary Free (מועדף) או Vercel Blob | אופטימיזציה למובייל |
| Auth | Auth.js | sessions מאובטחות |
| Auth משתמשים | Auth.js / מימוש פשוט credentials | אבטחה בסיסית |
| מייל לידים | Resend Free / חלופה חינמית | |
| WhatsApp | `wa.me` (ללא Business API) | |
| i18n | עברית באתר הציבורי | |
| אנליטיקס | Vercel Analytics או GA4 | רק אחרי הסכמת עוגיות אם לא-חיוני |

### 9.2 סביבות
| סביבה | שימוש |
|--------|--------|
| Local | פיתוח |
| Production | Vercel + Neon Postgres Free (`DATABASE_URL`) — פרויקט אמיתי |

Staging נפרד — לא חובה ב־MVP אם אין תקציב/זמן; אפשר preview deployments של Vercel.

### 9.3 דומיין
- Phase 1–2: subdomain חינמי של Vercel.
- Phase מאוחר: דומיין מותאם (`alex-nekasim.co.il` וכו') כשיהיה מוכן לעלות מחיר — **מחוץ לתקציב 0**.

### 9.4 משתני סביבה (לוגיים)
```
DATABASE_URL=
AUTH_SECRET=
CLOUDINARY_* / BLOB_*
RESEND_API_KEY= / EMAIL_*
ALEX_WHATSAPP_NUMBER=
ALEX_PHONE=
ALEX_NOTIFY_EMAIL=
NEXT_PUBLIC_SITE_URL=
```

---

## 10. אבטחה

### 10.1 הרשאות כתיבה
- רק בעל העסק (או המפתח) מפרסם נכסים; אין הגשת נכס ציבורית ללייב.
- API כתיבה מוגן בשרת; אין חשיפת שדות פרטיים ב־API/HTML ציבורי.

### 10.2 משתמשים ציבוריים (קל)
- הרשמה פשוטה; אין MFA.
- סיסמאות ב־hash בכל זאת.
- הגנה בסיסית מפני enumeration קיצוני — לא קריטי כמותית ב־MVP.

### 10.3 כללי
- HTTPS בכל מקום (Vercel).
- אין סודות ב־git.
- העלאת תמונות: סוגי קבצים מוגבלים (jpg/png/webp), מגבלת גודל.
- ולידציה בשרת לכל קלט.

---

## 11. נגישות (חובה — ישראל)

### 11.1 בסיס חוקי (תמצית ליישום)
- **חוק** שוויון זכויות לאנשים עם מוגבלות, התשנ״ח-1998.
- **תקנות** שוויון זכויות לאנשים עם מוגבלות (התאמות נגישות לשירות), התשע״ג-2013 (ותיקונים), לרבות סימן שירותי אינטרנט.
- **תקן ישראלי ת״י 5568** חלק 1 — מאמץ בפועל את **WCAG 2.0 ברמת AA** (עם התאמות לעברית/RTL).
- חובה לפרסם **הצהרת נגישות** במקום בולט, כולל דרכי יצירת קשר לבעיות נגישות ופרטי **רכז נגישות** אם נדרש למנות לפי החוק.
- קיימים פטורים מסוימים (למשל עוסק במחזור שנתי ממוצע נמוך מאוד לפי התקנות) — **הפרויקט מיישם נגישות כסטנדרט מלא בכל מקרה**, גם אם ייתכן פטור עסקי.

### 11.2 דרישות מוצר מחייבות
1. דף `/accessibility` — הצהרת נגישות.
2. ניווט מקלדת מלא; focus visible.
3. דילוג לתוכן (skip link).
4. ניגודיות מספקת (AA) על רקע כהה.
5. `alt` משמעותי לכל תמונת נכס ונלוות.
6. טפסים עם `<label>`, הודעות שגיאה מקושרות, סדר לוגי.
7. לא להסתמך על צבע בלבד לסטטוס/תגיות.
8. תמיכה ב־RTL ובזום דפדפן.
9. רכיבי UI נגישים (כפתורים אמיתיים, לא `div` מקליק בלבד).
10. בדיקות ידניות עם מקלדת + סריקת axe/lighthouse לפני שחרור.

### 11.3 מה לא מספיק
תוסף "כיפת נגישות" בלבד **אינו** תחליף להנגשה מובנית בקוד לפי התקן.

---

## 12. פרטיות והגנת מידע

### 12.1 בסיס חוקי (תמצית ליישום)
- **חוק הגנת הפרטיות**, התשמ״א-1981, כולל **תיקון 13** (בתוקף מ־2025).
- איסוף מידע אישי (שם, טלפון, אימייל, מועדפים, IP טכני) מחייב **שקיפות**, **מדיניות פרטיות**, ו־**הסכמה מדעת** במקומות הרלוונטיים.
- יידוע בעת איסוף: מטרה, בעל שליטה במאגר, זכויות עיון/תיקון/מחיקה, תוצאות אי-הסכמה, האם המסירה חובה.

### 12.2 דרישות מוצר מחייבות
1. דף `/privacy` — מדיניות פרטיות (טיוטה בסיסית ב־v1).
2. דף `/terms` — תנאי שימוש (טיוטה).
3. בכל טופס (ליד / הרשמה): **צ׳קבוקס הסכמה לא מסומן מראש** + קישור למדיניות.
4. שמירת `privacyConsentAt` בליד/משתמש.
5. ערוץ לבקשות עיון/תיקון/מחיקה (אימייל המפתח או אלכס — יוגדר בטיוטה).
6. עוגיות: דף `/cookies`; חיוניות לתפעול (session) ללא הסכמה עודפת; אנליטיקס לא-חיוני — רק אחרי הסכמה אם יוטמע. בטפסים — יידוע בעת איסוף + צ׳קבוקס.
7. מינימום נתונים: לא אוספים מה שלא צריך.
8. שדות בעל נכס — פנימיים לאלכס בלבד.

### 12.3 דיסקליימר עסקי
מחירים וזמינות כפופים לשינוי; המידע באתר אינו הצעה מחייבת; עסקאות רק דרך אלכס ובכפוף לחוק המתווכים.

### 12.4 רישיון תיווך
מספר רישיון התיווך של אלכס יוצג באתר (פוטר / אודות) — הפרט יגיע ב־DATA_CHECKLIST.

---

## 13. דרישות לא-פונקציונליות

| נושא | יעד |
|------|-----|
| ביצועים במובייל | LCP סביר; תמונות ממוטבות (responsive, modern formats) |
| זמינות | Vercel + Atlas free — best effort |
| SEO | בסיסי חינמי: title/description, OG לתמונת נכס, sitemap פשוט — **בלי** Schema.org RealEstate ב־MVP |
| בלוג | אין |
| מפה | אין ב־MVP (אולי בהמשך) |
| המלצות לקוחות | לא ב־MVP; יווספו אחרי איסוף תוכן |
| תמיכה בדפדפנים | Chrome/Safari/Firefox מובייל עדכניים |

---

## 14. שלבי מימוש

| Phase | תוצר | הערות |
|-------|------|--------|
| **0** | מסמכי אפיון + חוקים + checklist | הושלם |
| **1** | דמו UI ציבורי | mock / localStorage בלי DB |
| **2** | Neon Postgres + Auth אמיתי + העלאת תמונות + דיפלוי Vercel | חינם |
| **3** | תוכן אמיתי + משפטי | |
| **4** | בוט צ׳אט (§3.9) — **נדחה להמשך** לפי החלטת מוצר | לא בפריסה הנוכחית |
| **5** | המלצות, מפה (אופציונלי), הכנה לאפליקציה | נפרד |

### 14.1 היקף דמו (Phase 1) — חובה
- ניווט ציבורי בסיסי בעברית.
- רשימת נכסים + דף נכס + מסננים (על דאטה דמה).
- כפתורי WhatsApp + טפסים (יכולים להיות mock).

---

## 15. מחוץ להיקף (Out of Scope) — v1

- אפליקציית native / PWA מלאה (ייבחן אחרי הצלחת האתר)
- WhatsApp Business API (נשארים עם `wa.me`)
- בוט LLM מתקדם בתשלום / אינטגרציית CRM חיצונית
- מפת נכסים
- חיפוש טקסט חופשי
- CMS לתוכן סטטי
- אנגלית באתר הציבורי
- ריבוי מנהלים / הרשאות RBAC
- תשלומים / חוזים דיגיטליים
- ייצוא Excel/CSV (כרגע לא)
- בלוג / מאמרי SEO
- שליחת התראות אוטומטיות ללקוחות בלי פעולה של אלכס

**הועבר לתכנון (לא out-of-scope):** בוט צ׳אט ציבורי — ראה §3.9 ו־Phase 4.

---

## 16. סיכום החלטות שננעלו

ראה טבלת ההחלטות בתוכנית הפרויקט; כל סתירה עתידית בין קוד למסמך זה — **ה־SPEC גובר עד לעדכון מפורש של המסמך**.

---


## 18. נספח ב׳ — רשימת מסכים ציבוריים (צ׳קליסט)

- [ ] בית עם מותג חזק
- [ ] נכסים + מסננים
- [ ] דף נכס + גלריה + תגיות + WhatsApp + טופס
- [ ] ארכיון עסקאות
- [ ] צור קשר
- [ ] יש לי נכס
- [ ] הרשמה / התחברות / מועדפים
- [ ] פרטיות / תקנון / נגישות / דיסקליימר
- [ ] הצגת מספר רישיון תיווך
